# Install in a NEW environment; do not upgrade the reproduced DiT360 environment.
# Python 3.10+; install the CUDA-compatible torch wheel separately if required.
torch>=2.6,<3
diffusers==0.37.0
transformers==4.57.3
accelerate==1.12.0
peft==0.18.0
safetensors>=0.6.2,<0.8
huggingface-hub>=0.34,<1
datasets>=3.6,<5
pyarrow>=15
Pillow>=10,<13
numpy>=1.26,<3
sentencepiece>=0.2,<0.3
protobuf>=4,<7
